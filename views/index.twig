{#{% extends "base.twig" %}#}

{#{% block content %}#}

{#	{% for post in posts %}#}
{#		{% include ['tease-'~post.post_type~'.twig', 'tease.twig'] %}#}
{#	{% endfor %}#}
{#	#}
{#	{% include 'partial/pagination.twig' with { pagination: posts.pagination({show_all: false, mid_size: 3, end_size: 2}) } %}#}
{#{% endblock %}#}


{#{% extends "base.twig" %}#}

{#{% block content %}#}
{#	<!-- Grid Container -->#}
{#	<div class="container mx-auto p-4">#}
{#		<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">#}
{#			{% for post in posts %}#}
{#				<!-- Individual Post Container -->#}
{#				<div class="bg-white rounded-lg p-2 shadow overflow-hidden">#}
{#					{% include ['tease-'~post.post_type~'.twig', 'tease.twig'] %}#}
{#				</div>#}
{#			{% endfor %}#}
{#		</div>#}

{#		<!-- Pagination -->#}
{#		{% include 'partial/pagination.twig' with { pagination: posts.pagination({show_all: false, mid_size: 3, end_size: 2}) } %}#}
{#	</div>#}
{#{% endblock %}#}


{% extends "base.twig" %}

{% block content %}
	<!-- Grid Container -->
	<div class="container mx-auto p-4">
			{% include('views/marketing/features/feature-cards.twig') %}
		<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
			{% for post in posts %}
				<!-- Individual Post Container -->
				<div class="bg-white dark:bg-gray-600 dark:text-white rounded-lg p-4 shadow overflow-hidden">
					{% include ['tease-'~post.post_type~'.twig', 'tease.twig'] %}
				</div>
			{% endfor %}
		</div>

		<!-- Pagination -->
		{% include 'partial/pagination.twig' with { pagination: posts.pagination({show_all: false, mid_size: 3, end_size: 2}) } %}
	</div>
{% endblock %}
